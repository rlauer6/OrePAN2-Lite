package OrePAN2::Role::HasLogger;

use strict;
use warnings;

use OrePAN2::Logger;

use Role::Tiny;

our $VERSION = '@PACKAGE_VERSION@';

sub log { ## no critic
  my ($self) = @_;

  return $self->{log}
    if $self->{log};

  $self->{log} = OrePAN2::Logger->new;

  return $self->{log};
}

1;
